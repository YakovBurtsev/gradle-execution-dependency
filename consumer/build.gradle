task consume {
    doLast {
        def message = file("$rootDir/producer/build/generated/some.properties").text
        println("Consuming message: $message")
    }
}

dependencies {
    runtimeOnly project(':producer')
}